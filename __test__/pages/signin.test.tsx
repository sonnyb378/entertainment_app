import { render, screen, fireEvent } from '@testing-library/react'
import '@testing-library/jest-dom'
import {within} from '@testing-library/dom';


describe("test sign in page", () => {

    it("must display email address field", () => {

    })

    it("must display error if email is empty", () => {

    })

    it("must validate email address format", () => {

    })

    it("must display password field", () => {

    })

    it("must display error if password is empty", () => {

    })

    it("must display 'Sign In' button", () => {

    })

    it("must mock login", () => {

    })

    it("must not able to click 'Sign In' button if all fields are not filled", () => {

    })


    it("must display 'Sign In with Google' button", () => {
        
    })

    it("must mock Google login", () => {

    })
})